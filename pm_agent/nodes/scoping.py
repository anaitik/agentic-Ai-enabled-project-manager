"""Scoping node placeholder."""

from pm_agent.state import PMAgentState


def scoping_node(state: PMAgentState) -> PMAgentState:
    """Pass state through unchanged until scoping logic is implemented."""

    return state

